{
    "type":"object",
    "title":"Configuration settings of the app",
    "properties":{
        "isDevelopement":{
            "type":"boolean",
            "description":"This defines wheather the app is to be run in developement mode (true) or production mode (false)."
        },
        "theme":{
            "type":"string",
            "enum": ["dark", "light", "system"],
            "description":"Theme of the app"
        },
        "databaseFile":{
            "type":"object",
            "description": "This tells the application to where and how to find the database file.",
            "properties": {
                "location":{
                    "type":"string",
                    "description": "Location of the custom defined database file"
                },
                "isCustom":{
                    "type":"boolean",
                    "description": "Determines whether to use the custom defined database one or the default one."
                }
            }
        }
    }
}